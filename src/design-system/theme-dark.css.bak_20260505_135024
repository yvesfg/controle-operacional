/* ═══════════════════════════════════════════════════════════════════
   theme-dark.css — Mercury Dark Theme · Controle Operacional
   Ativação: adicione data-theme="dark" no <html> ou <body>
   ► Altere SOMENTE os valores aqui; nomes de variável ficam em tokens.css
   ═══════════════════════════════════════════════════════════════════ */

[data-theme="dark"],
.theme-dark {

  /* ── Backgrounds — Mercury surfaces ─────────────────────────── */
  --color-bg         : #171721;   /* Deep Space — fundo raiz */
  --color-bg-alt     : #1e1e2a;   /* Midnight Slate — fundo alternativo */
  --color-card       : #1e1e2a;   /* Midnight Slate — card nível 1 */
  --color-card-2     : #272735;   /* Graphite — card nível 2 / input */
  --color-header-bg  : #171721;
  --color-modal-bg   : #1e1e2a;
  --color-input-bg   : #272735;
  --color-table-header: #171721;

  /* ── Bordas ──────────────────────────────────────────────────── */
  --color-border     : rgba(112,112,125,.25);  /* Lead sutil */
  --color-border-2   : #70707d;                /* Lead */
  --color-border-focus: #5266eb;               /* Mercury Blue */

  /* ── Texto ───────────────────────────────────────────────────── */
  --color-text       : #ededf3;   /* Starlight */
  --color-text-2     : #c3c3cc;   /* Silver */
  --color-text-disabled: #70707d; /* Lead */
  --color-text-inverse: #ffffff;  /* Pure White — sobre botão azul */

  /* ── Acento principal — Mercury Blue ────────────────────────── */
  --color-primary    : #5266eb;
  --color-primary-dk : #3d4fc8;
  --color-primary-lt : #7080ef;
  --color-primary-bg : rgba(82,102,235,.12);
  --color-primary-border: rgba(82,102,235,.30);

  /* ── Status ──────────────────────────────────────────────────── */
  --color-success    : #22c55e;
  --color-success-dk : #16a34a;
  --color-success-bg : rgba(34,197,94,.10);
  --color-success-border: rgba(34,197,94,.25);

  --color-danger     : #ef4444;
  --color-danger-dk  : #dc2626;
  --color-danger-bg  : rgba(239,68,68,.10);
  --color-danger-border: rgba(239,68,68,.25);

  --color-warning    : #f59e0b;
  --color-warning-dk : #d97706;
  --color-warning-bg : rgba(245,158,11,.10);
  --color-warning-border: rgba(245,158,11,.25);

  --color-info       : #3b82f6;
  --color-info-lt    : #60a5fa;
  --color-info-bg    : rgba(59,130,246,.10);
  --color-info-border: rgba(59,130,246,.25);

  /* ── Overlays / Sombras ──────────────────────────────────────── */
  /* Elevação via cor, não sombra (Mercury guideline) */
  --color-shadow     : rgba(0,0,0,.45);
  --color-overlay    : rgba(0,0,0,.65);
  --color-scroll-thumb: #272735;

  --shadow-xs  : none;
  --shadow-sm  : none;
  --shadow-md  : none;
  --shadow-lg  : 0 8px 32px rgba(0,0,0,.50);
  --shadow-xl  : 0 16px 48px rgba(0,0,0,.60);
  --shadow-card: none;
  --shadow-primary: 0 4px 16px rgba(82,102,235,.25);

  /* ── Sidebar ─────────────────────────────────────────────────── */
  --color-sidebar-bg  : #171721;
  --color-sidebar-item: transparent;
  --color-sidebar-item-hover: rgba(82,102,235,.08);
  --color-sidebar-item-active: rgba(82,102,235,.15);
  --color-sidebar-text: #c3c3cc;
  --color-sidebar-text-active: #5266eb;

  /* ── Scrollbar ───────────────────────────────────────────────── */
  color-scheme: dark;

  /* ── Semantic Tokens — Mercury ───────────────────────────────── */
  /* Backgrounds */
  --bg      : #171721;          /* Deep Space */
  --surface : #1e1e2a;          /* Midnight Slate */
  --card    : #1e1e2a;          /* Midnight Slate */
  --card2   : #272735;          /* Graphite */

  /* Bordas */
  --border  : rgba(112,112,125,.25);   /* Lead sutil */
  --border2 : #70707d;                 /* Lead */

  /* Texto */
  --text    : #ededf3;          /* Starlight */
  --text2   : #c3c3cc;          /* Silver */
  --text3   : #70707d;          /* Lead */

  /* Acentos */
  --accent  : #5266eb;                  /* Mercury Blue */
  --accent2 : rgba(82,102,235,.15);     /* Mercury Blue bg leve */
  --cyan    : #06b6d4;          /* info / em rota */
  --green   : #22c55e;          /* sucesso / no prazo */
  --orange  : #f97316;          /* atenção / atraso */
  --red     : #ef4444;          /* alerta / crítico */
  --yellow  : #eab308;          /* aviso */
}
